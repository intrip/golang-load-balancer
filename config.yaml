# The load balancer server configuration
server:
  bind: '0.0.0.0'
  port: '8080'
  maxconnections: '100'
  # in seconds
  readTimeout: '30'
  # in seconds
  writeTimeout: '30'
# The list of server you want to balance on separated by comma
balancers: 'http://0.0.0.0:3003,http://0.0.0.0:3004'
